<h1> Remedy Tracker </h1>

<%= form_for @condition do |f| %>
    <%= f.label :user_id %>
    <%= f.collection_select :user_id, User.all, :id, :name %>

    <%= f.label :remedy_id %>
    <%= f.collection_select :remedy_id, Remedy.all, :id, :name %>

    <%= f.submit %>
<% end %>